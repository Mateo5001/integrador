@model IEnumerable<TallerAppServices.Models.ubicacionesDetalle>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.desDetalle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ubicaciones.desUbicacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.desDetalle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ubicaciones.desUbicacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idUbicacionDetalle }) |
            @Html.ActionLink("Details", "Details", new { id=item.idUbicacionDetalle }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idUbicacionDetalle })
        </td>
    </tr>
}

</table>
